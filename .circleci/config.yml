version: 2 # use CircleCI 2.0
jobs:
  build: # runs not using Workflows must have a `build` job as entry point
    
    working_directory: ~/circleci-wecmdb-core # directory where steps will run

    docker: # run the steps with Docker
      - image: circleci/openjdk:8-jdk-stretch # ...with this image as the primary container; this is where all `steps` will run

    steps: # a collection of executable commands

      - checkout # check out source code to working directory

      - restore_cache: # restore the saved cache after the first run or if `pom.xml` has changed
          key: circleci-wecmdb-core-{{ checksum "cmdb-core/pom.xml" }}
      
      - run: mvn -f cmdb-core/pom.xml dependency:go-offline # gets the project dependencies
      
      - save_cache: # saves the project dependencies
          paths:
            - ~/.m2
          key: circleci-wecmdb-core-{{ checksum "cmdb-core/pom.xml" }}
      
      - run: 
          command: mvn package -f cmdb-core/pom.xml # run the actual tests
          no_output_timeout: 30m
      
      - store_test_results: # uploads the test metadata from the `target/surefire-reports` directory so that it can show up in the CircleCI dashboard. 
          path: cmdb-core/target/surefire-reports
      
      - store_artifacts: # store the jar as an artifact
      # Upload test summary for display in Artifacts: https://circleci.com/docs/2.0/artifacts/
          path: cmdb-core/target/cmdb-core-0.0.1-SNAPSHOT.jar
